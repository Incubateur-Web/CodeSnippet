----

PARTIE DEBUG : VUEX ET STATES
à retirer quand PR

## back
branche api : setup-users-collection
normalement tout fonctionne de ce côté 

## front

en gros le formulaire login marche bien genre si on a les bons identifiants ça redirige bien (j'ai commenté la redirection justement pour debug)
si on se trouve bah error 404 on s'en fou pour l'instant de la gestion des messages d'erreurs

donc l'api renvoit si le login est bon : le token et l'username
au début ces deux datas étaient stockées dans le localstorage et à chaque accès à une page reservée aux utilisateurs connectés,
j'apppelais la fonction verifyToken dans les actions vuex du module auth.store.js
\src\store\modules\auth.store.js
et du coup ça renvoit ok ou pas ok et voila basta ça marchait très bien
sauf que du coup apparement c'est pas comme ça qu'il faut faire, et il faudrait utiliser les states vuex si déjà j'utilise les actions vuex

je connais ni vuejs et donc nullement vuex donc si je raconte du big shit c'est normal je ne sais pas ce que je fais

donc avec les data renvoyées par l'api je les renvoies vers le store vuex pour que le state soit mis à jour
et qu'on puisse récupérer le state partout dans l'appli sans avoir à checker le localstorage

j'ai tenté de le stocker dans le state vuex
et là c'est le drame : à chaque redirection ça réinitialise le state

le push précédent (ou celui d'avant je sais plus) est normalement la version sans l'utilisation des state, et elle semblait fonctionner correctement
donc si besoin il y a ça de dispo mais je doute que ça serve

avant de modifier la méthode d'inscription je voulais tester avec le login, le fonction d'inscription est hs actuellement
en gros j'arrive pas à set le state dans le store avec les mutations

il y a un problème avec l'action verifyToken aussi mais pour l'instant on peut s'en passer

---

pour tester le login: 
authtest@email.fr
123456789